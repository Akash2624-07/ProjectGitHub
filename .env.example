# Copy this file to .env and fill in a real token.
# GitHub personal access token (fine-grained, no scopes needed for public data).
# Optional — the app falls back to GitHub's unauthenticated rate limit (60 requests/hour) without it.
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here
